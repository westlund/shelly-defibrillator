# shelly-router-defibrillator
A Shelly Plug S Gen3 script that monitors a local router/firewall and performs a controlled power cycle only when the router itself stops responding. It is designed for cases where the internet may be down, but the router should not be restarted unless the router is actually frozen.
